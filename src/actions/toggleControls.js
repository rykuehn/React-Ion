export const TOGGLE_CONTROLS = 'TOGGLE_CONTROLS';

export function toggleControls() {
  // e.preventDefault();
  // e.stopPropagation();
  // console.log(e);
  return {
    type: TOGGLE_CONTROLS,
  };
}
